server:
  port: 8080

spring:
  application:
    name: message-processor-service
  liquibase:
    enabled: true
    change-log: classpath:changelog/changelog-test.xml
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
logging:
  level:
    root: INFO
    io.openleap: DEBUG